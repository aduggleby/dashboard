<button class="help-button" type="button" aria-label="Open help" title="Help" @@click="openHelp()">
    ?
</button>

<section x-cloak x-show="helpOpen" class="add-card-modal" aria-label="Dashboard help dialog" @@click.self="closeHelp()">
    <div class="add-card-modal__panel help-modal__panel" role="dialog" aria-modal="true" aria-labelledby="help-dialog-title" @@keydown.escape.window="closeHelp()">
        <header class="help-dialog__header">
            <h2 id="help-dialog-title">Dashboard Help</h2>
            <button class="help-dialog__close" type="button" aria-label="Close help" @@click="closeHelp()">Close</button>
        </header>

        <div class="help-dialog__section">
            <h3>Card Actions</h3>
            <ul>
                <li>Add card: select the <code>+</code> button in the top-right, then submit the form.</li>
                <li>Edit or delete card: right-click a card to open <code>Edit app card</code>.</li>
                <li>Open app: left-click a card.</li>
            </ul>
        </div>

        <div class="help-dialog__section">
            <h3>Reordering</h3>
            <ul>
                <li>Drag and drop cards to reorder.</li>
                <li>A preview slot appears while dragging to show the drop position.</li>
                <li>Keyboard shortcut: focus a card, then <code>Alt+ArrowUp</code> or <code>Alt+ArrowDown</code>.</li>
            </ul>
        </div>

        <div class="help-dialog__section">
            <h3>Top Bar Controls</h3>
            <ul>
                <li><code>+</code>: open the Add app card modal.</li>
                <li><code>⚙</code>: open dashboard settings and rename the dashboard title.</li>
                <li><code>Sun/Moon</code> icon: toggle theme override for this browser.</li>
                <li><code>?</code>: open this help modal.</li>
            </ul>
        </div>

        <div class="help-dialog__section">
            <h3>Theme</h3>
            <ul>
                <li>Theme defaults to your system light/dark mode.</li>
                <li>Use the theme toggle in the header to override per browser.</li>
            </ul>
        </div>

        <div class="help-dialog__section">
            <h3>Help Shortcuts</h3>
            <ul>
                <li>Open help: <code>?</code> or <code>F1</code></li>
                <li>Close help: <code>Esc</code></li>
            </ul>
        </div>
    </div>
</section>
